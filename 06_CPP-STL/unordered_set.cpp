#include <iostream>
#include <unordered_set>

using namespace std;

int main() {
    // stores only unique elements but in random order

    unordered_set<int> us;
    us.insert(1);

    // rest same as set and multiset

    return 0;
}